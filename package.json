{
  "name": "color-api",
  "version": "1.0.0",
  "description": "A simple color api.",
  "main": "public/index.php",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "start": "php -S localhost:8080 -t public/",
    "test": "php ./vendor/bin/phpunit --colors=always tests/",
    "prettier": "prettier --/ --php-version=8.0 -c"
  },
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "@prettier/plugin-php": "^0.17.6",
    "prettier": "^2.6.0"
  }
}
